<!DOCTYPE html>
<html lang="en">
  <head>
      <meta charset="UTF-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Document</title>

      <%= stylesheet_link_tag "loginPage", "data-turbo-track": "reload" %>


  </head>
  <body>

    <section class="vh-100">
      <div class="container-fluid h-custom">
        <div class="row d-flex justify-content-center align-items-center h-100">
          <div class="col-md-9 col-lg-6 col-xl-5">
            <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-login-form/draw1.webp"
              class="img-fluid" alt="Sample image">
          </div>
          <div class="col-md-8 col-lg-6 col-xl-4 offset-xl-1">


            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <form>
              <div class="d-flex flex-row align-items-center justify-content-center justify-content-lg-start">
                <p class="lead fw-normal mb-0 me-3">Sign in with</p>
                <button type="button" class="btn btn-primary btn-floating mx-1">
                  <i class="fab fa-facebook-f"></i>
                </button>

                <button type="button" class="btn btn-primary btn-floating mx-1">
                  <i class="fab fa-twitter"></i>
                </button>

                <button type="button" class="btn btn-primary btn-floating mx-1">
                  <i class="fab fa-linkedin-in"></i>
                </button>
              </div>

              <div class="divider d-flex align-items-center my-4">
                <p class="text-center fw-bold mx-3 mb-0">Or</p>
              </div>

              <!-- Email input -->

              <div class="form-outline mb-4">
                <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder:"Email", class:"form-control form-control-lg" %>
              </div>

              <!-- Password input -->
              <div class="form-outline mb-3">
                <%= f.password_field :password, autocomplete: "current-password", placeholder:"Enter Password", class:"form-control form-control-lg"%>
              </div>

              <div class="d-flex justify-content-between align-items-center">
                <!-- Checkbox -->
                <div class="form-check mb-0">

                  <% if devise_mapping.rememberable? %>
                    <div>
                      <%= f.check_box :remember_me, class:"form-check-input me-2" %>
                      <%= f.label :remember_me, class: "form-check-label" %>
                    </div>
                  <% end %>

                </div>
                <%= link_to "Forgot password?", new_password_path(resource_name), class:"text-body" %>
              </div>

              <div class="text-center text-lg-start mt-4 pt-2">
                <%= f.submit "Login", class: "btn btn-warning btn-lg", style: "padding-left: 2.5rem; padding-right: 2.5rem;" %>
            <% end %>

                <p class="small fw-bold mt-2 pt-1 mb-0">

                  Don't have an account? <%= link_to "Register", new_registration_path(resource_name), class: "link-danger" %></p>
              </div>

            </form>

             
          </div>
        </div>
      </div>
    </section>
  </body>
</html>








